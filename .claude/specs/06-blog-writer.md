# Optimized Blog Writer

*In admin/ideas -> list all the ideas and create write new button
*create admin/ideas/write route
*in /write route make full fledge article writer which have:
 -> Markdown Support
 -> Text positioning(left, right, center)
  -> Upload images(seo optimized) through links or uplaod from device
  -> for now use image link implementation
  -> when user uplaod the image link then automatically fetch the image, make it dragable, edit the description
  -> Images position should be dragable or position should be left, right center
  -> Images description for seo optimizaion
  -> adjust paragraph
  -> don't compromise seo (imporve it with xhigh effort)
  -> admin credential ("kingnit" as user, "root" as password) if necessary